#include<iostream>
using namespace std;
int main()
{
    //while loop:Here it first checks the condition if it satisfies the condition then it executes the code inside the loop
    /*int index=1;
    while (index<=5)
    {
        cout<<index<<endl;
        index++;
    } */

    // do while loop :do while loop atleast execute once the required code and then checks the condition 
    /*int index=6;
    do{
        cout<<index<<endl;
        index++;
    } while (index <=5); 
    */
    return 0;
}